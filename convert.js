// Generated by CoffeeScript 1.3.1
var add_inputs, available_chars, blank, cancel, draw, enter, esc, input, keymap, space, store, tag,
  __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

tag = function(id) {
  return document.getElementById(id);
};

keymap = '';

available_chars = 'abcdefghijjklmnopqrstuvwxyz';

available_chars += 'ABCDEFGHIJJKLMNOPQRSTUVWXYZ';

available_chars += '1234567890!@#$%^&*()';

available_chars += '~`_-+-[]{}\\|:;"\',.<>/?';

add_inputs = available_chars.split('');

draw = function(arr) {
  var item, str, _i, _len;
  str = '';
  for (_i = 0, _len = arr.length; _i < _len; _i++) {
    item = arr[_i];
    console.log(item);
    if (typeof item === 'object') {
      str += draw(item);
    } else if (item === '\t') {
      console.log('xxxxx');
      str += '<nav>&nbsp;</nav>';
    } else {
      str += "<span>" + item + "</span>";
    }
  }
  return "<div>" + str + "</div>";
};

window.onload = function() {
  var box, nothing, refresh;
  box = tag('box');
  nothing = tag('nothing');
  (refresh = function() {
    return box.innerHTML = draw(store);
  })();
  nothing.onkeypress = function(e) {
    var char;
    char = String.fromCharCode(e.keyCode);
    if (__indexOf.call(add_inputs, char) >= 0) {
      console.log("(" + char + ") in inputs");
      input(char);
      refresh();
    }
    return false;
  };
  return nothing.onkeydown = function(e) {
    var code;
    code = e.keyCode;
    console.log(code);
    switch (code) {
      case 8:
        console.log('cancel');
        cancel();
        refresh();
        return false;
      case 13:
        console.log('enter');
        enter();
        refresh();
        return false;
      case 32:
        console.log('space');
        space();
        refresh();
        return false;
      case 9:
        console.log('tab');
        blank();
        refresh();
        return false;
      case 27:
        console.log('esc');
        esc();
        refresh();
        return false;
    }
  };
};

store = [1, 2, [[1, [3, 4, 5], 3], '\t', 3, [2, 3, 4]]];

input = function(char) {
  return store.push(char);
};

enter = function() {
  return '';
};

space = function() {
  return '';
};

cancel = function() {
  return '';
};

blank = function() {
  return '';
};

esc = function() {
  return '';
};
